class Split{getInfo(){return{id:'Split',name:'Split',blocks:[{opcode:'Split',blockType:Scratch.BlockType.REPORTER,text:'Use [splitter] to split [text] and extract data [datanum]',arguments:{text:{type:Scratch.ArgumentType.STRING,defaultValue:'Hello world!'},splitter:{type:Scratch.ArgumentType.STRING,defaultValue:' '},datanum:{type:Scratch.ArgumentType.NUMBER,defaultValue:'1'}}},]}}Split(args){let t=args.text;let s=args.splitter;let n=args.datanum;let d=t.split(s);return d[n]}}Scratch.extensions.register(new Split());